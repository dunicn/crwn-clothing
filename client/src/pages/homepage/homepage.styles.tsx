import styled, { StyledComponent } from 'styled-components';

export const HomePageContainer: StyledComponent<
  'div',
  any,
  {},
  never
> = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
`;
